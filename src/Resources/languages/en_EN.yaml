# Main component
link:
  title: Link
  add_link: Add link
  delete_all: Delete all links
  label:
    title: Label
    placeholder: Put your link label here.
  relationship:
    title: Relationship
    description: "You can set the <code>nofollow</code> value to avoid bots following the linked document."
  target:
    title: Target
    blank: Opens the linked document in a new window or tab
    self: Opens the linked document in the same frame as it was clicked
    parent: Opens the linked document in the parent frame
    top: Opens the linked document in the full body of the window
  website:
    title: Title
    address: Web address
    placeholder: "http://"
    goto: Go to website
